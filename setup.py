from setuptools import setup, find_packages

setup(name='vocabulary_book',
      version='1.0',
      description='vocabulary book server',
      author='Pham',
      license='MIT',
      packages=find_packages()
      )